package com.bits.loanproposal.domain.enums;

/** A normal Java enum persisted by Spring Data MongoDB using its constant name. */
public enum LoanProposalTestOrigin {
    BRANCH,
    MOBILE_APP,
    PARTNER_API
}
